# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Loanville2"

[setup]
script = '''
#!/usr/bin/env bash
set -euo pipefail

# Codex should provide the worktree/repo path via an env var.
# Try common ones; fall back to the "worktree created at ..." path isn't available here.
REPO_DIR="${CODEX_WORKTREE:-${CODEX_REPO_ROOT:-${PWD}}}"

cd "$REPO_DIR"

if [ ! -d ".venv" ]; then
  python3 -m venv .venv
fi
source .venv/bin/activate

python -m pip install -U pip
python -m pip install -r requirements.txt
'''
